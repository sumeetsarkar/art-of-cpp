int main() {
    int foo = 10;
    int _foo = 20;
    int FOO = 30;
    int Foo99 = 99;

    /*
     * Rules for variable naming
     * 1. Only have [A-Z] or [a-z] or numbers [0-9] or _ (underscore)
     * 2. Can only begin with `_` or alphabet
     * 3. Case sensitive
     * 4. Cannot contain space
     * 5. Cannot use keywords as identifiers
     * */
}

